{
  "name": "@lizhengyi/openrouter-image-mcp",
  "version": "0.1.1",
  "description": "Generate and save images through OpenRouter from MCP clients.",
  "type": "module",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Zhengyi003/openrouter-media-mcp.git"
  },
  "homepage": "https://github.com/Zhengyi003/openrouter-media-mcp",
  "bin": {
    "openrouter-image-mcp": "dist/index.js"
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "scripts": {
    "clean:build": "node --input-type=module --eval \"import fs from 'node:fs'; fs.rmSync('dist', { recursive: true, force: true })\"",
    "clean:test": "node --input-type=module --eval \"import fs from 'node:fs'; fs.rmSync('.test-dist', { recursive: true, force: true })\"",
    "build": "npm run clean:build && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && npm run clean:test && tsc -p tsconfig.test.json && node --test .test-dist/tests/**/*.test.js",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "25.0.10",
    "typescript": "5.9.3"
  }
}